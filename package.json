{
  "name": "bstocks-launchpad",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat run scripts/test_suite.js && hardhat run scripts/smoke.js",
    "deploy": "hardhat run scripts/deploy.js --network bsc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^2.1.3",
    "dotenv": "^17.4.2",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0"
  }
}
